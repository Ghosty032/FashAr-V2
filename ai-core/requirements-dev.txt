# FASHR AI Core — development and CI dependencies
#
# Kept separate from requirements.txt so the deployed image does not carry test tooling.
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0

# Used only by ai-core/scripts/update_product_weights.py, which runs as a scheduled job
# rather than inside the web service. See .github/workflows/update-product-weights.yml
supabase==2.28.0
