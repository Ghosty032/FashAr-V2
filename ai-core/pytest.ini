[pytest]
testpaths = tests
asyncio_mode = auto

# Integration tests hit live Pinecone and need a seeded index, so they are excluded from
# the default run. Include them with:  pytest -m integration
addopts = -m "not integration" -q

markers =
    integration: talks to a live external service (needs credentials)
