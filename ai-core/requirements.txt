# FASHR AI Core — runtime dependencies
#
# Pinned deliberately. An earlier unpinned deploy resolved a transitive dependency that
# would not build on Render (simsimd, via langchain-pinecone) and had to be fixed in
# production. Versions below are what the project is developed and tested against.
#
# Target runtime is Python 3.11 (see runtime.txt).

# --- Web layer ---
fastapi==0.135.1
uvicorn[standard]==0.41.0
python-multipart==0.0.22          # required by FastAPI to parse multipart/form-data
python-dotenv==1.2.2
pydantic==2.12.5

# --- LangChain / LangGraph ---
langchain-core==1.2.17
langchain-nvidia-ai-endpoints==1.1.0
langgraph==1.0.10

# --- Vector store ---
# Pinecone 7.x is required: integrated inference (create_index_for_model, upsert_records,
# index.search) does not exist in earlier majors.
pinecone==7.3.0

# --- HTTP ---
# Imported directly by app/services/weather_service.py. It previously resolved only as a
# transitive dependency of langsmith, which meant an upstream change could have removed it.
httpx==0.28.1
